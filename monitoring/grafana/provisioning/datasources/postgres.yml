apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres-master:5432
    database: otus_hw
    user: otus_hw
    secureJsonData:
      password: otus_hw
    isDefault: true
    jsonData:
      sslmode: disable
      maxOpenConns: 100
      maxIdleConns: 100
      connMaxLifetime: 14400 